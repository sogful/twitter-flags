{
  // top row
  "dev": [
    {"key": "jfDev", "label": "jetfuel dev mode", "title": "sets sessionStorage jfDev, unlocking the super epic emusk rocket emoji mode"},
    {"key": "inspect", "label": "inspector", "title": "not actually a twitter feature, but hover any element to see its data-testid!"},
    {"key": "exposeDebug", "label": "expose audio", "title": "adds ?exposeDebug=1 so the media stream audio player is reachable"},
    {"key": "forceDevEnv", "label": "force dev environment", "title": "tries to revive prod-gated developer code (needs a reload)"}
  ],
  // filter row
  "filters": [
    {"key": "true", "label": "enabled"},
    {"key": "safe", "label": "safe"},
    {"key": "danger", "label": "dangerous"},
    {"key": "mod", "label": "modified"}
  ],
  "actions": [
    {"action": "unregister", "label": "unregister sw.js", "title": "unregister the service worker (fresh assets when reloading)"},
    {"action": "flush", "label": "flush cache", "title": "delete the cached html app shell"},
    {"action": "refresh", "label": "refresh cache", "title": "reprime the html app shell"}
  ]
}
